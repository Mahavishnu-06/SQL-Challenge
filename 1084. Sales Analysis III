select Distinct a.product_id,a.product_name from Product a join Sales b on a.product_id =b.product_id where a.product_id in (select product_id from Sales group by product_id having min(sale_date)>="2019-01-01" and max(sale_date)<="2019-03-31");
