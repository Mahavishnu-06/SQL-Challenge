with answer as(select id,recordDate,temperature,Lag(temperature) over(order by recordDate) as prev, Lag(recordDate) over(order by recorddate) as date From Weather)
select id from answer where temperature>prev and DateDiff(recordDate,date)=1;
